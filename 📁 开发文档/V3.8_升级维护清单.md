# V3.8 升级维护清单

> 基于 V3.7.2 复查清单制定的修复 + 优化路线图
> 制定日期: 2026-02-13
> 原则: 先保资金安全，再补功能缺陷，最后优化体验

---

## 阶段 0: 数据现状确认（先查再改）

> 动手修代码之前，先确认当前运行时数据的状态

| # | 检查项 | 命令 | 预期 | 实际 | 状态 |
|---|--------|------|------|------|------|
| 0.1 | position_bot_map 格式 | `cat ~/.openclaw/workspace/position_bot_map.json` | 全部为 `{"bot_id":..., "strategy":...}` 字典 | | [ ] |
| 0.2 | order_bot_map 格式 | `cat ~/.openclaw/workspace/order_bot_map.json` | 全部为 `{"bot_id":..., "symbol":..., "strategy":...}` | | [ ] |
| 0.3 | risk_state.json bot_daily_pnl | `cat ~/.openclaw/workspace/risk_state.json` | `bot_daily_pnl` 应有数据(修复前可能为空) | | [ ] |
| 0.4 | sl_placed.json 残留 | `cat ~/.openclaw/workspace/sl_placed.json` | 只有当前有持仓的品种 | | [ ] |
| 0.5 | evolution net_profit 范围 | 见复查清单检查 5 命令 | 数值范围一致(全百分比或全USDT) | | [ ] |
| 0.6 | thresholds.json | `cat ~/.openclaw/workspace/stats/thresholds.json` | bot_thresholds 三个 bot 都有 | | [ ] |
| 0.7 | 当前持仓 | `curl -s localhost:8092/positions` | 与币安 Demo 一致 | | [ ] |
| 0.8 | 交易状态 | `curl -s localhost:8092/trading/status` | trading_enabled 状态正确 | | [ ] |

---

## 阶段 1: P0 紧急修复（资金安全）

> 目标: 修复直接影响资金的 2 个 Bug，改动最小化

### 1.1 修复 position_bot_map 冲突检测 (Bug #1)

**文件**: `execution-service/src/__main__.py`

**修改内容**:
```
第 328 行:
旧: existing_bot = executor._position_bot_map.get(norm_sym)
新: existing_bot = executor.get_position_bot_id(norm_sym)
```

**测试**:
- [ ] 启动 execution-service
- [ ] 发送 al-brooks 的 SOLUSDT 开仓请求 → 成功
- [ ] 再发送 trader 的 SOLUSDT 开仓请求 → 被 "品种冲突" 拒绝
- [ ] 再发送 al-brooks 的 SOLUSDT 加仓请求 → 成功（同 bot 不拦截）

### 1.2 修复 close_position bot 级 PNL 记录 (Bug #2)

**文件**: `execution-service/src/executor.py`

**修改内容**:
```
第 662-663 行:
旧:
    if response.success:
        self.risk_manager.record_pnl(pos.unrealized_pnl)
新:
    if response.success:
        bot_id = self.get_position_bot_id(symbol)
        if bot_id:
            self.risk_manager.record_bot_pnl(bot_id, pos.unrealized_pnl)
        else:
            self.risk_manager.record_pnl(pos.unrealized_pnl)
```

**测试**:
- [ ] 手动平仓一个持仓
- [ ] 检查 `risk_state.json` 中 `bot_daily_pnl[bot_id]` 有数据
- [ ] 检查 `bot-summary` API 的 `daily_pnl.realized` 非零
- [ ] 累计测试: 设小额日亏限，连续亏损后确认开仓被拒

### 1.3 阶段 1 验收

- [ ] 两个修复都已应用
- [ ] execution-service 正常启动无报错
- [ ] `curl localhost:8092/health` 返回 healthy
- [ ] Git 提交: `fix(V3.8-P0): 冲突检测类型修复 + bot级PNL记录`

---

## 阶段 2: P1 功能修复

> 目标: 修复功能缺陷，确保核心工作流完整

### 2.1 signal-router max_turns 改回 6 (Bug #5)

**文件**: `~/.openclaw/transforms/signal-router.js`

**修改内容**: 3 处 `max_turns: 3` → `max_turns: 6`
- 第 802 行 (routeToAlBrooks)
- 第 875 行 (routeToWyckoff)
- 第 950 行 (routeToTrader)

**测试**:
- [ ] 重启 OpenClaw Gateway
- [ ] 发送测试信号
- [ ] 观察 agent 日志确认 6 轮都可用
- [ ] 确认 agent 完成 分析→下单→笔记→更新 完整流程

### 2.2 统一 PNL 单位为 USDT (Bug #3)

**文件**: `execution-service/src/order_tracker.py`

**修改内容**:
```
第 215-219 行 (_determine_exit_reason):
旧:
    if direction == "long":
        pnl_pct = ((exit_price - entry_price) / entry_price) * 100
    else:
        pnl_pct = ((entry_price - exit_price) / entry_price) * 100
新:
    quantity = trade.get("quantity", 0)
    if direction == "long":
        pnl_usdt = (exit_price - entry_price) * quantity
        pnl_pct = ((exit_price - entry_price) / entry_price) * 100
    else:
        pnl_usdt = (entry_price - exit_price) * quantity
        pnl_pct = ((entry_price - exit_price) / entry_price) * 100
```

```
第 221-229 行 (OrderStatusChange):
旧: pnl=pnl_pct
新: pnl=pnl_usdt, pnl_percent=pnl_pct

注: OrderStatusChange.pnl 语义从百分比改为 USDT 金额
    新增 pnl_percent 字段用于显示和笔记回填
```

```
第 147 行 (进化记录):
确认: evo.record_trade_result(pnl=change.pnl) — 现在传的是 USDT ✓
```

**数据迁移**: 考虑重置 `*_evolution.json` 中的 `net_profit` 字段为 0（旧数据混合了两种单位）

**测试**:
- [ ] 手动关闭一个持仓
- [ ] 检查 order_tracker 日志中 pnl 值是 USDT 金额
- [ ] 检查 evolution 文件中 net_profit 增量合理

### 2.3 modify-sl 迁移到软件止损 (Bug #4)

**文件**: `execution-service/src/__main__.py`

**修改范围**: 第 1080-1135 行整个函数重写

**核心逻辑**:
1. 如果有原生止损单（真实账户），尝试取消
2. 更新 `sl_placed.json` 中对应品种的止损价
3. 如果巡检有移动止损状态（`_trailing_state`），同步更新

**测试**:
- [ ] `curl -X POST 'localhost:8092/order/SOLUSDT:USDT/modify-sl?new_stop_loss=150'`
- [ ] 检查 sl_placed.json 更新
- [ ] 等待巡检运行，确认新止损价被识别

### 2.4 recover_bot_map 写新格式 (Bug #6)

**文件**: `execution-service/src/executor.py`

**修改内容**:
```
第 257 行:
旧: self._position_bot_map[norm_sym] = val["bot_id"]
新: self._position_bot_map[norm_sym] = {"bot_id": val["bot_id"], "strategy": val.get("strategy", "auto")}

第 279 行:
旧: self._position_bot_map[norm_sym] = bot_id
新: self._position_bot_map[norm_sym] = {"bot_id": bot_id, "strategy": "auto"}
```

**测试**:
- [ ] 重启 execution-service
- [ ] 检查 `position_bot_map.json` 格式
- [ ] 如果有持仓，确认恢复后全部为字典格式

### 2.5 阶段 2 验收

- [ ] 4 个修复都已应用
- [ ] 完整流程测试: 信号→分析→下单→止损→平仓→进化记录→笔记回填
- [ ] `curl localhost:8092/health` 返回 healthy
- [ ] 同步更新 `openclaw-config/transforms/signal-router.js`
- [ ] Git 提交: `fix(V3.8-P1): max_turns恢复6 + PNL统一USDT + modify-sl软件止损 + bot_map新格式`

---

## 阶段 3: P2 次要修复 + 代码清理

### 3.1 软件止损触发时记录进化 (Bug #7)

**文件**: `position_patrol.py` 第 334-338 行

**修改**: 在 `unregister_position` 后添加进化记录和 bot PNL 记录

**测试**:
- [ ] 设置一个很近的止损价让巡检触发
- [ ] 检查 evolution 文件有新记录
- [ ] 检查 risk_state.json 的 bot_daily_pnl 更新

### 3.2 _backfill_note 空值保护 (Bug #8)

**文件**: `order_tracker.py` 第 436 行和第 456 行

**修改**: 增加 None 检查，outcome 增加 "未知" 选项

### 3.3 删除重复函数 (Bug #9)

**文件**: `signal-router.js` 第 227-239 行
**修改**: 删除第一个 `getStrategyEvolution` 定义

### 3.4 阶段 3 验收

- [ ] 所有修复已应用
- [ ] 无新增报错
- [ ] Git 提交: `fix(V3.8-P2): 止损进化记录 + 空值保护 + 重复函数清理`

---

## 阶段 4: 架构优化（非紧急，择机做）

### 4.1 冷却期检查完善 (问题 A)

**方案**: bot-summary 增加 `all_cooldowns` 字段
- 从 `risk_state.json` 的 `bot_trade_times` 直接计算所有品种冷却
- 不限于当前持仓品种

### 4.2 笔记门禁标准化 (问题 C)

**方案**: 明确三档标准并同步到 router + SKILL.md
- `< min_score`: 不推送
- `min_score ~ trade_score`: Discord 一行摘要
- `>= trade_score`: 完整分析 + 下单 + 笔记

### 4.3 进化数据清洗

**方案**: 编写一次性脚本
1. 读取所有 `*_evolution.json`
2. 检测 `net_profit` 值是否合理（如果绝对值 < 10 大概率是百分比，> 10 大概率是 USDT）
3. 统一为 USDT 或重置为 0

### 4.4 位图/数据清理工具 (position_bot_map 格式统一)

**方案**: 启动时增加格式迁移
- 检测所有字符串格式的值，迁移为字典格式
- 类似 `_load_order_bot_map` 中已有的旧格式迁移逻辑（第 82-96 行）

---

## 阶段 5: 真实账户准备（远期）

> 从 Demo 切换到真实账户前必须完成的清单

### 5.1 恢复原生 STOP_MARKET 条件委托

**涉及文件和位置**:
| 文件 | 行号 | 当前实现 | 目标实现 |
|------|------|---------|---------|
| `executor.py` | 573-593 | 写入 sl_placed.json | exchange.create_order(type='stop_market') |
| `position_patrol.py` | 286-296 | 写入 sl_placed | exchange.create_order(type='stop_market') |
| `position_patrol.py` | 462-478 | 更新 sl_placed | exchange.create_order + cancel 旧单 |

**保留 sl_placed.json 作为二级备份**:
- 原生止损单为主（交易所服务器执行，更可靠）
- sl_placed.json 为辅（防止原生止损单异常丢失）
- 巡检同时检查两者

### 5.2 进程守护

**方案**: PM2 或 systemd 管理 execution-service
```bash
pm2 start "python -m src --port 8092" --name exec-svc --cwd ~/path/to/execution-service
pm2 save
pm2 startup
```

### 5.3 API Key 安全

- [ ] 真实 API Key 不硬编码在 config.py
- [ ] 使用环境变量或加密存储
- [ ] IP 白名单配置

### 5.4 资金分配复核

- [ ] 重新计算各 bot 的 allocated_usdt / max_notional
- [ ] 调整日亏限至真实账户合理值
- [ ] 确认 MAX_LEVERAGE 不超过账户允许值

### 5.5 上线前灰度测试

- [ ] 先用极小资金（$50）测试全流程
- [ ] 观察一周确认无异常
- [ ] 逐步增加资金

---

## 附录: 文件修改汇总

| 阶段 | 文件 | 修改行 | 类型 |
|------|------|--------|------|
| P0-1.1 | `__main__.py` | 328 | 1 行修改 |
| P0-1.2 | `executor.py` | 662-667 | 5 行修改 |
| P1-2.1 | `signal-router.js` | 802,875,950 | 3 处数字改动 |
| P1-2.2 | `order_tracker.py` | 215-229 | 15 行修改 |
| P1-2.3 | `__main__.py` | 1080-1135 | 50 行重写 |
| P1-2.4 | `executor.py` | 257,279 | 2 行修改 |
| P2-3.1 | `position_patrol.py` | 334-343 | 10 行新增 |
| P2-3.2 | `order_tracker.py` | 436,456 | 2 行修改 |
| P2-3.3 | `signal-router.js` | 227-239 | 13 行删除 |

**总计**: ~100 行改动，覆盖 5 个文件

---

## 执行记录

> 每完成一个阶段在此记录

| 日期 | 阶段 | 操作 | Git Commit | 备注 |
|------|------|------|------------|------|
| | 阶段 0 | 数据现状确认 | — | |
| | 阶段 1 | P0 修复 | | |
| | 阶段 2 | P1 修复 | | |
| | 阶段 3 | P2 修复 | | |
| | 阶段 4 | 架构优化 | | |
