> **⚠️ 历史文档 (2026-01 重组前)** — 本文记录的路径和架构可能已过时，仅供参考。当前项目结构请查看 `📁 开发文档/PROJECT_STRUCTURE.md`。

# 迁移进度追踪

> 最后更新: 2026-01-29

---

## 总体进度

```
Phase 1: 基础设施 ████████████████████░░░░░ 85%
├── Web Dashboard 框架     ✅ 完成
├── WebSocket 服务         ✅ 完成
├── Docker 配置            ✅ 完成
├── 后端控制面板           ✅ 完成
└── 策略引擎               ✅ 完成（后端集中计算方案）

Phase 2: 核心功能 ████████████░░░░░░░░░░░░░ 50%
├── K线图表                🟡 进行中（基础完成，待接入真实数据）
├── 策略信号               ✅ 后端计算完成，Web端已接入
├── 市场扫描               ⚪ 待开始
└── 信号监控               ⚪ 待开始

Phase 3: 策略系统          🟡 进行中 40%
├── 策略引擎 (后端)        ✅ 完成
├── 策略规则库             ✅ 完成（6个策略）
├── 策略API                ✅ 完成
└── 策略配置               ⚪ 待开始

Phase 4: 数据同步          ⚪ 未开始 0%
Phase 5: Obsidian 减负     ⚪ 未开始 0%
```

---

## 已完成项目

### 1. Web Dashboard (tradecat-dashboard/)

**框架搭建** ✅
- [x] Next.js 14 + TypeScript + Tailwind CSS
- [x] 响应式布局（侧边栏 + 主内容区）
- [x] 深色主题（与 Obsidian 一致）
- [x] 路由结构（7 个页面）

**组件** ✅
- [x] Sidebar 侧边栏导航
- [x] Header 顶部栏
- [x] ConnectionStatus 连接状态
- [x] BackendControl 后端控制面板
- [x] TradingViewChart K线图表组件

**Hooks** ✅
- [x] useWebSocket - WebSocket 连接管理
- [x] useCandles - K线数据获取
- [x] **useStrategySignals - 策略信号（从后端获取）** ⭐新增

**基础设施** ✅
- [x] WebSocket 客户端封装
- [x] 类型定义（与 Obsidian 兼容）
- [x] Docker 开发/生产配置

### 2. WebSocket 服务 (backend/.../websocket-service/)

**核心功能** ✅
- [x] FastAPI + Socket.IO 服务器
- [x] 房间管理（按品种订阅）
- [x] K线数据订阅/取消订阅
- [x] 信号订阅/取消订阅
- [x] 心跳保活机制
- [x] 健康检查端点

**策略引擎** ✅ ⭐新增
- [x] 策略规则库（6个策略）
  - H1突破
  - L1突破
  - EMA20缺口
  - 布林带反弹
  - 趋势强度
  - 成交量突增
- [x] 策略引擎（技术指标计算）
- [x] 信号分析 API

**部署** ✅
- [x] Dockerfile
- [x] docker-compose.yml
- [x] 启动/停止脚本

### 3. 策略同步方案

**方案确定** ✅ ⭐重要
- [x] 后端集中计算架构
- [x] 策略规则库（Python）
- [x] API 设计
- [x] Web Dashboard 接入
- [x] 文档（策略同步方案.md）

### 4. 启动脚本

- [x] `start-all.sh` - 一键启动所有服务
- [x] `stop-all.sh` - 一键停止所有服务

---

## 关键设计决策

### 策略同步方案：后端集中计算 ✅

```
策略逻辑只在一处（后端 Python）
    ↓
Obsidian 和 Web Dashboard 只做展示
    ↓
修改策略只需改一处，两端自动同步
```

详见：[策略同步方案.md](./策略同步方案.md)

---

## 进行中项目

### A. 接入真实数据源（当前优先级）

**任务**:
- [ ] WebSocket 服务接入 data-service 获取真实 K线
- [ ] Web Dashboard 接入 WebSocket 实时数据
- [ ] 策略引擎使用真实数据计算信号

### B. 市场扫描仪

**待开发**:
- [ ] SymbolCard 组件
- [ ] 市场网格布局
- [ ] 实时价格更新

### C. 信号监控面板

**待开发**:
- [ ] 信号列表展示
- [ ] 浏览器通知
- [ ] 信号历史

---

## 下一步任务（按优先级）

### 🔴 P0: 数据源接入（本周）

1. **WebSocket 服务接入 data-service**
   ```python
   # handlers/candles.py
   # 修改 _fetch_latest_candle() 从 data-service 获取真实数据
   ```

2. **验证策略信号**
   - 使用真实 K线数据
   - 验证策略计算结果
   - 对比 Obsidian 端输出

### 🟡 P1: 市场扫描仪（下周）

1. 迁移 SymbolGrid 组件
2. 实现实时价格更新
3. 品种筛选功能

### 🟢 P2: Obsidian 减负（后续）

1. 标记弃用功能
2. 添加 Web 端引导
3. 分版本移除代码

---

## 快速启动指南

```bash
# 1. 启动所有服务（在项目根目录）
./start-all.sh

# 2. 访问 Web Dashboard
open http://localhost:3000

# 3. 查看连接状态（侧边栏底部）
#    - HTTP API 状态
#    - WebSocket 状态
#    - 启动/停止按钮

# 4. 查看策略信号
#    访问 /chart 页面
#    输入品种代码（如 BTCUSDT）
#    查看图表上的策略标记

# 5. 停止所有服务
./stop-all.sh
```

---

## API 测试

### 测试策略信号 API

```bash
# 获取 BTCUSDT 的策略信号
curl "http://localhost:8088/api/v1/signals/analyze?symbol=BTCUSDT&interval=5m"

# 获取可用策略列表
curl "http://localhost:8088/api/v1/strategies"
```

---

## 项目结构

```
Al-brooks-PA/
├── tradecat-dashboard/              # Web Dashboard
│   ├── src/
│   │   ├── app/
│   │   ├── components/
│   │   ├── hooks/
│   │   │   └── useStrategySignals.ts  ⭐ 新增
│   │   └── lib/
│   └── docker-compose.yml
│
├── backend/tradecat-core/
│   └── services/websocket-service/
│       ├── src/
│       │   ├── main.py
│       │   ├── strategies/           ⭐ 新增
│       │   │   ├── __init__.py
│       │   │   ├── rules.py          # 策略规则库
│       │   │   └── engine.py         # 策略引擎
│       │   └── handlers/
│       └── docker-compose.yml
│
├── 🦁 交易员控制台 (Trader Command)/
│   └── docs/
│       ├── 架构分析-三分离方案.md
│       ├── 迁移计划-详细执行方案.md
│       ├── 迁移进度.md               # 本文档
│       └── 策略同步方案.md           ⭐ 新增
│
├── start-all.sh
└── stop-all.sh
```

---

## 待解决问题

| 问题 | 优先级 | 状态 | 备注 |
|------|--------|------|------|
| WebSocket 接入真实数据源 | P0 | 待处理 | 需修改 handlers/candles.py |
| 策略信号验证 | P0 | 待处理 | 对比 Obsidian 输出 |
| 市场扫描仪开发 | P1 | 待开始 | 下周任务 |
| Obsidian 策略迁移 | P1 | 待规划 | 可选：Obsidian 也调用后端 |
| 数据同步机制 | P2 | 待规划 | 交易记录、笔记同步 |

---

## 备注

- ✅ **策略同步方案已确定**：后端集中计算，Obsidian/Web 只做展示
- ✅ **后端策略引擎已完成**：6个策略已迁移到 Python
- ✅ **Web Dashboard 已接入策略 API**
- 🟡 **当前阻塞**：需接入真实 K线数据源
