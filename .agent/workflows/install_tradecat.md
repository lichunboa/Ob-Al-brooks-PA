---
description: How to install TradeCat cleanly
---

1. Clone the repository
2. Run `./scripts/init.sh`
3. Copy config: `cp config/.env.example config/.env`
4. Run verification: `./scripts/verify.sh`
