# 🎯 插件优化项目 - 目标架构

> **版本**: v2.0.0  
> **创建日期**: 2026-01-07  
> **最后更新**: 2026-01-11  
> **状态**: 规划中

---

## 📚 文档导航

**快速上下文恢复**: [📚 项目技术文档-完整架构手册](./📚%20项目技术文档-完整架构手册.md) - 新窗口AI必读

| 文档 | 用途 |
|------|------|
| [📚 项目技术文档](./📚%20项目技术文档-完整架构手册.md) | 完整代码库清单、开发路线图、质量指标 |
| **本文档** | 最终目标状态、验收标准、UI设计规范 |
| [📋 主任务列表](./📋%20插件功能优化-主任务列表.md) | Phase 1-5 详细任务分解 |
| [🧠 优化技能](./🧠%20插件智能优化技能.md) | 优化方法论、成功经验、失败教训 |

---

## 📖 文档说明

这个文档定义了插件优化的**最终目标状态**,包括:
- 文件结构
- 模块职责
- UI 设计规范
- 数据流
- 命名规范

**为什么需要这个文档?**
- 明确优化的方向和终点
- 避免优化过程中迷失方向
- 确保所有优化都朝着同一个目标
- 作为验收标准

---

## 🗂️ 目标文件结构

### 当前状态 (v1.0.0-lts)
```
src/
├── main.ts                    # 插件入口
├── core/                      # 核心业务逻辑
│   ├── coach.ts              # Coach 模块
│   ├── course.ts             # 课程模块
│   ├── sm2.ts                # SM2 算法
│   └── strategy-matcher.ts   # 策略匹配
├── platforms/                 # 平台适配层
│   └── obsidian/
│       ├── obsidian-trade-index.ts
│       └── ...
├── ui/                        # UI 组件和样式
│   ├── components/
│   │   └── DesignSystem.tsx
│   └── styles/
│       └── theme.ts
└── views/                     # 视图层
    ├── Dashboard.tsx          # 🔴 7,624 行 - 需要拆分
    ├── tabs/
    │   ├── LearnTab.tsx      # ✅ 已拆分
    │   └── ManageTab.tsx     # ✅ 已拆分
    └── components/
        ├── manager/
        └── strategy/
```

### 目标状态 (v2.0.0)
```
src/
├── main.ts                    # 插件入口 (保持不变)
│
├── core/                      # 核心业务逻辑 (保持不变)
│   ├── coach.ts
│   ├── course.ts
│   ├── sm2.ts
│   └── strategy-matcher.ts
│
├── platforms/                 # 平台适配层 (保持不变)
│   └── obsidian/
│       ├── obsidian-trade-index.ts
│       └── ...
│
├── utils/                     # 🆕 工具函数 (纯函数)
│   ├── trade-calculations.ts # 交易计算函数
│   ├── date-utils.ts         # 日期工具
│   ├── format-utils.ts       # 格式化工具
│   └── chart-utils.ts        # 图表工具
│
├── hooks/                     # 🆕 自定义 Hooks
│   ├── useDashboardData.ts   # Dashboard 数据
│   ├── useTradeData.ts       # 交易数据
│   ├── useAnalytics.ts       # 分析数据
│   └── useLearnData.ts       # 学习数据
│
├── ui/                        # UI 组件和样式
│   ├── components/            # 通用 UI 组件
│   │   ├── DesignSystem.tsx
│   │   ├── Card.tsx          # 🆕 卡片组件
│   │   ├── KPITile.tsx       # 🆕 KPI 瓦片
│   │   └── Button.tsx        # 🆕 按钮组件
│   └── styles/
│       ├── theme.ts
│       └── global.css        # 🆕 全局样式
│
└── views/                     # 视图层
    ├── Dashboard.tsx          # ✅ 减少到 ~2000 行 (只保留布局和状态管理)
    │
    ├── tabs/                  # Tab 组件
    │   ├── TradingHubTab.tsx # 🆕 交易中心 Tab
    │   ├── AnalyticsTab.tsx  # 🆕 数据分析 Tab
    │   ├── LearnTab.tsx      # ✅ 已拆分
    │   └── ManageTab.tsx     # ✅ 已拆分
    │
    └── components/            # Tab 内的子组件
        ├── trading/           # 🆕 Trading Hub 组件
        │   ├── TodayStatus.tsx
        │   ├── KPIGrid.tsx
        │   ├── RecentTrades.tsx
        │   └── StrategyAssistant.tsx
        │
        ├── analytics/         # 🆕 Analytics 组件
        │   ├── AccountOverview.tsx
        │   ├── CalendarHeatmap.tsx
        │   ├── TrendChart.tsx
        │   └── Filters.tsx
        │
        ├── learn/             # Learn 组件 (已有)
        │   └── ...
        │
        └── manage/            # Manage 组件 (已有)
            └── ...
```

---

## 📊 文件大小目标

| 文件 | 当前 | 目标 | 减少 |
|------|------|------|------|
| Dashboard.tsx | 7,624 行 | ~2,000 行 | -74% |
| TradingHubTab.tsx | 0 行 | ~500 行 | 新建 |
| AnalyticsTab.tsx | 0 行 | ~500 行 | 新建 |
| LearnTab.tsx | 已拆分 | ~500 行 | ✅ |
| ManageTab.tsx | 已拆分 | ~500 行 | ✅ |
| utils/ | 0 行 | ~1,000 行 | 新建 |
| hooks/ | 0 行 | ~800 行 | 新建 |
| **总计** | 16,945 行 | ~12,000 行 | -30% |

---

## 🎨 UI 设计目标

### 设计原则

1. **信息层级清晰**
   - Page 标题(图标 + 模块名)
   - 主卡片(核心任务/核心状态)
   - 次级区块(明细/列表/建议)

2. **模块边界明确**
   - 卡片容器 + 内部分区
   - 同类信息使用网格
   - 可扫描的左侧锚点

3. **视觉语言统一**
   - 对比靠明度/层级,不靠线条/阴影
   - 语义色只用于点状强调
   - 标题靠字重 + 间距,不靠巨大字号

### 通用结构(每个 Tab 都遵循)

```
┌─────────────────────────────────────────┐
│ 📊 模块标题 (Module Header)              │
│ 左侧: 图标 + 中文标题                    │
│ 右侧: 英文副标题或简短说明(弱化)          │
├─────────────────────────────────────────┤
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 主卡片 (Primary Card)                │ │
│ │ 承载模块最重要的状态与动作            │ │
│ │ - 分区标题                           │ │
│ │ - 留白节奏                           │ │
│ │ - 不靠粗边框/重阴影                  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌───────┬───────┬───────┐              │
│ │ KPI 1 │ KPI 2 │ KPI 3 │ (KPI Tiles) │
│ │ label │ label │ label │              │
│ │ value │ value │ value │              │
│ └───────┴───────┴───────┘              │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 主 CTA (Primary CTA)                 │ │
│ │ 每个模块最多 1 个主 CTA              │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 列表/明细 (Secondary)                   │
│ - 放在主卡之后                          │
│ - 避免跟 KPI 抢注意力                   │
└─────────────────────────────────────────┘
```

### 可复用 CSS Class

```css
/* 模块标题 */
.pa-module-header
.pa-module-title
.pa-module-subtitle

/* 卡片 */
.pa-card                    /* 模块主容器 */
.pa-section-title          /* 卡内分区标题 */

/* KPI */
.pa-kpi-grid               /* KPI 网格容器 */
.pa-kpi-grid-3            /* 3列网格 */
.pa-kpi-grid-2            /* 2列网格 */
.pa-kpi-tile              /* 单个 KPI 瓦片 */
.pa-kpi-label             /* KPI 标签(小且灰) */
.pa-kpi-value             /* KPI 值(大但不吵) */
.pa-kpi-value.lg          /* 大号 KPI 值 */

/* 按钮 */
.pa-button-primary        /* 主按钮 */
.pa-button-secondary      /* 次按钮 */
.pa-button-text           /* 文本按钮 */
```

### 颜色规范

**中性色**(跟随 Obsidian 主题):
```css
--background-primary      /* 主背景 */
--background-secondary    /* 次背景 */
--background-modifier-border  /* 边框 */
--text-normal            /* 正常文本 */
--text-muted             /* 弱化文本 */
--text-faint             /* 极弱文本 */
--interactive-accent     /* 强调色 */
```

**语义色**(v5 固定色值,不改):
```css
--pa-v5-live             /* 实盘 */
--pa-v5-demo             /* 模拟 */
--pa-v5-backtest         /* 回测 */
--pa-v5-win              /* 盈利 */
--pa-v5-loss             /* 亏损 */
```

---

## 🔄 模块职责划分

### Dashboard.tsx (主控制器)
**职责**: 
- Tab 切换逻辑
- 全局状态管理
- 布局和路由

**不应该包含**:
- 具体的业务逻辑
- 数据计算
- UI 渲染细节

**代码示例**:
```typescript
export function Dashboard() {
  const [activeTab, setActiveTab] = useState('trading');
  
  return (
    <div className="pa-dashboard">
      <TabNavigation active={activeTab} onChange={setActiveTab} />
      
      {activeTab === 'trading' && <TradingHubTab />}
      {activeTab === 'analytics' && <AnalyticsTab />}
      {activeTab === 'learn' && <LearnTab />}
      {activeTab === 'manage' && <ManageTab />}
    </div>
  );
}
```

---

### utils/ (纯函数)
**职责**:
- 数据计算
- 格式化
- 工具函数

**特点**:
- 无副作用
- 可测试
- 可复用

**代码示例**:
```typescript
// trade-calculations.ts
export function calculateWinRate(trades: Trade[]): number {
  if (trades.length === 0) return 0;
  return trades.filter(t => t.outcome === 'win').length / trades.length;
}

export function calculateProfitLoss(trades: Trade[]): number {
  return trades.reduce((sum, t) => sum + t.pnl, 0);
}
```

---

### hooks/ (自定义 Hooks)
**职责**:
- 数据获取
- 状态管理
- 副作用处理

**特点**:
- 封装复杂逻辑
- 可复用
- 遵循 React Hooks 规范

**代码示例**:
```typescript
// useDashboardData.ts
export function useDashboardData() {
  const [trades, setTrades] = useState<Trade[]>([]);
  const [loading, setLoading] = useState(true);
  
  useEffect(() => {
    const loadData = async () => {
      setLoading(true);
      const data = await tradeIndex.getAllTrades();
      setTrades(data);
      setLoading(false);
    };
    loadData();
  }, []);
  
  return { trades, loading };
}
```

---

### Tab 组件
**职责**:
- 组织子组件
- 传递数据
- 处理 Tab 级别的状态

**不应该包含**:
- 具体的 UI 实现(委托给子组件)
- 复杂的计算(委托给 utils)
- 数据获取(委托给 hooks)

**代码示例**:
```typescript
// TradingHubTab.tsx
export function TradingHubTab() {
  const { trades, loading } = useTradeData();
  const recentTrades = trades.slice(0, 10);
  
  return (
    <div className="pa-trading-hub">
      <TodayStatus trades={trades} />
      <KPIGrid trades={trades} />
      <RecentTrades trades={recentTrades} />
      <StrategyAssistant />
    </div>
  );
}
```

---

### 子组件
**职责**:
- 专注的 UI 渲染
- 接收 props
- 触发回调

**特点**:
- 单一职责
- 可复用
- 易于测试

**代码示例**:
```typescript
// KPIGrid.tsx
interface KPIGridProps {
  trades: Trade[];
}

export function KPIGrid({ trades }: KPIGridProps) {
  const winRate = calculateWinRate(trades);
  const pnl = calculateProfitLoss(trades);
  
  return (
    <div className="pa-kpi-grid pa-kpi-grid-3">
      <KPITile label="胜率" value={`${(winRate * 100).toFixed(1)}%`} />
      <KPITile label="盈亏" value={formatCurrency(pnl)} />
      <KPITile label="交易数" value={trades.length} />
    </div>
  );
}
```

---

## 📋 命名规范

### 文件命名
- **组件**: PascalCase (例如: `TradingHubTab.tsx`)
- **工具函数**: kebab-case (例如: `trade-calculations.ts`)
- **Hooks**: camelCase with `use` prefix (例如: `useDashboardData.ts`)

### 变量命名
- **组件**: PascalCase (例如: `TradingHubTab`)
- **函数**: camelCase (例如: `calculateWinRate`)
- **常量**: UPPER_SNAKE_CASE (例如: `MAX_TRADES`)
- **接口**: PascalCase with `I` prefix (例如: `ITrade`)

### CSS Class 命名
- **模块**: `pa-` prefix (例如: `pa-dashboard`)
- **BEM 风格**: `block__element--modifier` (例如: `pa-kpi-tile__label--large`)

---

## 🎯 验收标准

### 代码质量
- [ ] Dashboard.tsx < 2,000 行
- [ ] 每个 Tab < 500 行
- [ ] 每个子组件 < 200 行
- [ ] 每个函数 < 50 行
- [ ] 重复代码率 < 5%
- [ ] 无循环依赖

### 功能完整性
- [ ] 所有现有功能保持正常
- [ ] 无新增 bug
- [ ] 性能没有退化

### UI 一致性
- [ ] 所有 Tab 使用统一的结构
- [ ] 所有卡片使用统一的样式
- [ ] 所有按钮使用统一的组件
- [ ] 颜色符合规范

### 可维护性
- [ ] 代码结构清晰
- [ ] 职责划分明确
- [ ] 易于添加新功能
- [ ] 易于修复 bug

---

## 📚 相关文档

- [📋 插件优化项目-主任务列表](./📋%20插件优化项目-主任务列表.md)
- [🧠 插件智能优化技能](./🧠%20插件智能优化技能.md)
- [📋 UI 迭代技能（插件控制台）](./📋%20UI%20迭代技能（插件控制台）.md)
- `.claude/skills/plugin-smart-optimization/`

---

**最后更新**: 2026-01-07 17:10  
**状态**: 规划完成,等待执行
