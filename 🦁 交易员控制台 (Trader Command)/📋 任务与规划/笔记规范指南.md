# 📘 笔记规范指南 - 让插件识别你的数据

本文档帮助你理解如何规范编写笔记，让 **交易员控制台** 的 V3 智能引擎能够正确识别和推荐。

---

## 🎯 核心原则

1. **使用双语字段名**：如 `市场周期/market_cycle`
2. **值来自预设**：所有字段值必须来自 `Templates/属性值预设.md`
3. **控制台只读 frontmatter**：正文内容不会被自动解析

---

## 📝 三大笔记类型

### 1. 交易记录 (Trade Note)

**模版位置**：`Templates/单笔交易模版 (Trade Note).md`

**关键字段** (V3 引擎消费)：

| 字段 | 作用 | 示例值 |
|------|------|--------|
| `账户类型/account_type` | 筛选器分类 | `实盘 (Live)` / `模拟 (Demo)` |
| `品种/ticker` | 分组显示 | `ES` / `BTC` / `NQ` |
| `市场周期/market_cycle` | 策略推荐 | `强趋势 (Strong Trend)` |
| `方向/direction` | 策略推荐 | `做多 (Long)` / `做空 (Short)` |
| `设置类别/setup_category` | 属性完善 | `趋势回调 (Trend Pullback)` |
| `观察到的形态/patterns_observed` | 策略匹配 | `楔形顶底 (Wedge Top/Bottom)` |
| `信号K/signal_bar_quality` | K线验证 | `顺势K线 (Trend Bar)` |
| `策略名称/strategy_name` | 历史胜率 | `高1/低1 (High 1/Low 1)` |
| `结果/outcome` | 统计分析 | `止盈 (Win)` / `止损 (Loss)` |
| `净利润/net_profit` | 盈亏计算 | `125.50` |

**示例 frontmatter**：
```yaml
---
账户类型/account_type: 实盘 (Live)
品种/ticker: ES
市场周期/market_cycle: 弱趋势 (Weak Trend)
方向/direction: 做多 (Long)
设置类别/setup_category: 趋势回调 (Trend Pullback)
观察到的形态/patterns_observed:
  - 高1/低1 (High 1/Low 1)
信号K/signal_bar_quality: 顺势K线 (Trend Bar)
策略名称/strategy_name: 高1/低1 (High 1/Low 1)
结果/outcome: 止盈 (Win)
净利润/net_profit: 150.00
---
```

---

### 2. 策略卡片 (Strategy Card)

**模版位置**：`Templates/策略卡片规范模板 (Strategy Card Template).md`

**关键字段** (V3 引擎消费)：

| 字段 | 作用 | 示例值 |
|------|------|--------|
| `策略名称/strategy_name` | 推荐显示 | `急赴磁体 (Rush to Magnet)` |
| `市场周期/market_cycle` | 策略匹配 | `交易区间 (Trading Range)` |
| `方向/direction` | 方向过滤 | `做多 (Long)` |
| `观察到的形态/patterns_observed` | 形态关联 | `急赴磁体 (Rush to Magnet)` |

**存放位置**：`策略仓库 (Strategy Repository)/`

---

### 3. 属性值预设 (Enum Presets)

**位置**：`Templates/属性值预设.md`

这是**唯一的值来源**。插件启动时会解析此文件，生成下拉菜单选项。

**规则**：
- 每个字段下的列表项即为合法值
- 使用 `中文 (English)` 双语格式
- 新增值必须在此文件添加

---

## ⚠️ 常见错误

| 错误 | 正确 |
|------|------|
| `market_cycle: 强趋势` | `市场周期/market_cycle: 强趋势 (Strong Trend)` |
| `direction: Long` | `方向/direction: 做多 (Long)` |
| `patterns_observed: wedge` | `观察到的形态/patterns_observed: 楔形顶底 (Wedge Top/Bottom)` |
| 自创新值 | 从 `属性值预设.md` 复制 |

---

## 🔗 V3 引擎数据链路

```
策略仓库 ────────────────────────────┐
  ↓ 读取 frontmatter                 │
  ↓ 提取 market_cycle, patterns     │
                                     ├→ V3 推荐引擎
交易记录 ────────────────────────────┤
  ↓ 读取 frontmatter                 │
  ↓ 计算胜率、匹配历史               │
                                     │
SRS 卡片 ────────────────────────────┘
  ↓ 读取 interval (掌握度)
  ↓ 影响推荐权重
```

---

## ✅ 检查清单

- [ ] 使用双语字段名 (`中文/english`)
- [ ] 值来自 `属性值预设.md`
- [ ] 交易记录有 `账户类型`、`品种`、`市场周期`
- [ ] 策略卡片有 `策略名称`、`市场周期`、`形态`
- [ ] 新增值已在 `属性值预设.md` 添加

---

> 💡 **提示**：使用 Obsidian 的模版插件 (Templater) 可以快速插入规范的 frontmatter。
